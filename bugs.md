- [x] Sometimes the notifications are too big they take a lot of the screen (Fixed: Streamlined width to 380px)
- [x] The nofifications should be below the calendar and time (Fixed: Centered dropdown with detailed calendar on top and notifications directly below)
- [x] The ui for wifi bluetooth and so on control is stright up ass (Fixed: Removed quick settings grid and sliders completely from notifications dropdown)
When there are two windows open for example the current "Focued" window should have a highlight color to know the currently focused one
Using the CTRL+SUPER+Arrows do indeed change the windows to another workspace and works as expected, however there is a little bug where for example lets assume there are terminal one on the left and terminal two on the right, when using the mentioned shortcut the terminals become one at the top and another on the bottom instead of them being on the left and right, so make moving the workspaces also keep the windows location as they used to be without changing them


The top left island numbers changing should have animation (Risky as I had fought a lot with Gemini)

# Questions
How can I change windows location, for example ter1:left-up, ter2:right-top, ter3:left-bottom, ter4:right-bottom . I am currently at ter2 and I want to switch places with ter1, how can I do that using shortcuts