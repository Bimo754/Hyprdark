# ==============================================================================
# Hyprdark - Visual Design, Input & Animation Engine
# High-contrast dark cyber theme with directional metallic/cyber accents.
# ==============================================================================

general {
    gaps_in = 6
    gaps_out = 12
    border_size = 2

    # Apple-style clean monochromatic subtle border (no gradients)
    col.active_border = rgba(ffffff30)
    col.inactive_border = rgba(2c2c2e55)

    layout = dwindle
    resize_on_border = true
    allow_tearing = false
}

decoration {
    rounding = 12

    active_opacity = 0.98
    inactive_opacity = 0.92

    shadow {
        enabled = true
        range = 28
        render_power = 4
        color = rgba(00000055)
    }

    blur {
        enabled = true
        size = 6
        passes = 3
        new_optimizations = true
        contrast = 0.95
        brightness = 0.85
        noise = 0.015
    }
}

animations {
    enabled = true

    # Clean, low-latency industrial easing (no bouncy or floating delays)
    bezier = cyberSnap, 0.05, 0.95, 0.1, 1.0
    bezier = linear, 0.0, 0.0, 1.0, 1.0

    animation = windows, 1, 3, cyberSnap, popin 85%
    animation = windowsOut, 1, 3, cyberSnap, popin 85%
    animation = border, 1, 4, linear
    animation = fade, 1, 3, cyberSnap
    animation = workspaces, 1, 3, cyberSnap, slide
    animation = specialWorkspace, 1, 3, cyberSnap, slidevert
}

input {
    kb_layout = us
    numlock_by_default = true
    follow_mouse = 1
    sensitivity = 0

    touchpad {
        natural_scroll = true
        tap-to-click = true
        disable_while_typing = true
    }
}

dwindle {
    pseudotile = true
    preserve_split = true
}

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
    disable_splash_rendering = true
    background_color = 0x0d0e15
    focus_on_activate = true
}
